import React from 'react';
import { Text,View } from 'react-native';

export const LiveCard = (params) => {
  return(
      <View>
          <Text>
              LiveCard
          </Text>
      </View>
  )
};
;