import React from 'react';
import { Text,View } from 'react-native';

export const ProfileHeader = (params) => {
  return(
      <View>
          <Text>
              ProfileHeader
          </Text>
      </View>
  )
};
;